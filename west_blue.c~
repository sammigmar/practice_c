#include <stdio.h>
/**
 *This is a format specificer example
 *We will be going over format specificiers
 *Of course as well as data types and variables
 */

int main ()
{
float item1 = 1.75;
float item2 = 3.50;
float item3 = 8.25;

printf("Item 1: $%f", item1);
 return 0;
}
