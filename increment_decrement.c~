#include <stdio.h>
/**
 * I will be going over increments and decrements in tandum with format specification as well as arithmetic operators
 * I will also be touching on augmented assignment operators for comparison with increments and decrements
 */

int main()

{
  int x = 10;
  x+=2;
  x++;
  printf("My name is Sam\n I am %d years old\n", x);
  return 0;
}
