#include <stdio.h>

/**
 * Today we will be going over Arithmetic Operators
 * Aka basic number linguistics in C coding language
 */

int main()
{
  int x = 10; /**
initialization and decleration all in one.
	      */
  int y = 45;
  int z = x * y; /**
  the value of variable z is the byproduct of variables x and y being multiplied by each other
		 */

  printf("%d\n", z);

  return 0;
}
