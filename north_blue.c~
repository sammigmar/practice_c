#include <stdio.h>

int main()
{
  /**
   * We will be going over constand = fixed value that cannot be altered by the program during its execution
   */

 const  float PI = 3.14159
    
    printf("%F", PI);

  return 0;
}
